const chai = require('chai');

chai.should();
const { expect } = chai;

describe('Module placeholder', () => {
  describe('function placeholder', () => {
    it('should succeed', async () => {
      expect(true).to.be.equal(true);
    });
  });
});
